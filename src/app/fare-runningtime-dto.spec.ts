import { FareRunningtimeDto } from './fare-runningtime-dto';

describe('FareRunningtimeDto', () => {
  it('should create an instance', () => {
    expect(new FareRunningtimeDto()).toBeTruthy();
  });
});
